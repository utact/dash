<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.dash.board.infrastructure.persistence.BoardMapper">

  <resultMap id="BoardResult" type="com.ssafy.dash.board.domain.Board">
    <id property="id" column="id" />
    <result property="title" column="title" />
    <result property="content" column="content" />
    <result property="userId" column="user_id" />
    <result property="createdAt" column="created_at" />
    <result property="updatedAt" column="updated_at" />
  </resultMap>

  <insert id="insert" parameterType="com.ssafy.dash.board.domain.Board" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO boards (title, content, user_id, created_at, updated_at)
    VALUES (#{title}, #{content}, #{userId}, #{createdAt}, #{updatedAt})
  </insert>

  <select id="selectById" resultMap="BoardResult" parameterType="long">
    SELECT id, title, content, user_id, created_at, updated_at FROM boards WHERE id = #{id}
  </select>

  <select id="selectAll" resultMap="BoardResult">
    SELECT id, title, content, user_id, created_at, updated_at FROM boards ORDER BY id DESC
  </select>

  <update id="update" parameterType="com.ssafy.dash.board.domain.Board">
    UPDATE boards
    SET title = #{title}, content = #{content}, updated_at = #{updatedAt}
    WHERE id = #{id}
  </update>

  <delete id="delete" parameterType="long">
    DELETE FROM boards WHERE id = #{id}
  </delete>

</mapper>

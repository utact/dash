<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.dash.board.infrastructure.mapper.BoardMapper">

  <insert id="insert" parameterType="Board" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO boards (title, content, user_id, created_at, updated_at)
    VALUES (#{title}, #{content}, #{userId}, #{createdAt}, #{updatedAt})
  </insert>

  <select id="selectById" resultType="Board" parameterType="long">
    SELECT * FROM boards WHERE id = #{id}
  </select>

  <select id="selectAll" resultType="Board">
    SELECT * FROM boards ORDER BY id DESC
  </select>

  <update id="update" parameterType="Board">
    UPDATE boards SET title = #{title}, content = #{content}, updated_at = #{updatedAt} WHERE id = #{id}
  </update>

  <delete id="delete" parameterType="long">
    DELETE FROM boards WHERE id = #{id}
  </delete>

</mapper>
